# Variables de entorno para producción (Railway)
# Railway proporcionará automáticamente DATABASE_URL para PostgreSQL
DATABASE_URL=${{Railway.DATABASE_URL}}
JWT_SECRET=${{Railway.JWT_SECRET}}
PORT=${{Railway.PORT}}
NODE_ENV=production
